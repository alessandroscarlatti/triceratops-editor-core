const assert = require('assert');
const something = require("./something").stuff.things;

describe('Array', function() {
    describe('#indexOf()', function() {
        it("something can be imported", function() {
            console.log("something", something);
        })
    });
});